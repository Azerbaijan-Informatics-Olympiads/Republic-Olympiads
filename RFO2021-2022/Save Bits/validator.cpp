#include "testlib.h"
#include <bits/stdc++.h>

using namespace std;

int main(int argc, char** argv){
  registerValidation(argc, argv);
  const int MAXN = 20;
  int N = inf.readInt(1, MAXN, "N");
  inf.readEoln();
  inf.readEof();
  return 0;
}
